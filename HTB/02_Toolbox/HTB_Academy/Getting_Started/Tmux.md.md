`tmux` — Start a new session. 
`Ctrl+b` then `%` — Split screen vertically (Attack on one side, logs on the other).
`tmux attach` or `tmux attach -t [specific session]` — Reconnect to your session if your internet drops (Critical for long labs).
`tmux list-session` — See all active session
